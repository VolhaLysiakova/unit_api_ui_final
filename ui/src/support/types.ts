export enum PAGES {
    HOME = "Home",
    LOBBY = "Lobby",
    TOURNAMENTS = "Tournaments"
}

export enum ELEMENT_TYPES {
    INPUT = "input",
    BUTTON = "button",
    LABEL = "label",
    TOGGLE = "toggle"
}

export enum ACCOUNT_TYPES {
    FREE = "Free",
    FOA = "FOA"
}

export enum BOARD_COLOR {
    STANDARD = "Standard",
    WORLD_CHESS = "World Chess",
    TEXTBOOK = "Textbook"
}

export enum BUTTON_STATUS {
    ENABLED = "enabled",
    DISABLED = "disabled",
}
